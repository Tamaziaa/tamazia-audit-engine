```diff
@@ -61,7 +61,7 @@
   try {
     const result = await readDocument(url, pageClass, opts);
     if (result.reason === 'gone') {
-      // FAIL-CLOSED: fetch failure/timeout on a FOUND policy link is UNREAD content, logged AND returned unread:true; the lane
+      // FAIL-OPEN: fetch failure/timeout on a FOUND policy link is UNREAD content, logged AND returned unread:true; the lane
       return result;
     }
     // ... rest of function
```

```diff
@@ -275,7 +275,7 @@
       const claim = buildClaim(f, opts);
       await runEntailment(claim, opts, remaining);
     } catch (e) {
-      // FAIL-OPEN: runEntailment's catch demotes to needs_review, never throws into the mint
+      // FAIL-OPEN: runEntailment's catch demotes to needs_review, never throws into the mint
       stampNliDemote(f, { verdict: 'error', reason: e.message });
     }
   }
 }
```
