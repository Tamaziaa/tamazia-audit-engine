**C-257** (2026-07-18, P3) <`scripts/validate‑config.js` applied `JSON.parse` directly to `config/defaults.json`, which contained developer‑added `//` comments; the parser threw a `SyntaxError` when the file was loaded, halting the build.> -> <All JSON configuration files must be parsed with a comment‑tolerant parser (e.g., `json5` or a pre‑processor that strips comments) or must be validated to contain only strict JSON syntax before parsing.>

**C-258** (2026-07-18, P3) <`scripts/bundle.js` wrote a raw template string containing user‑supplied plugin names into the output bundle without HTML‑escaping; when the bundle was embedded in an HTML page, a plugin name like `<script>alert('xss')</script>` could execute arbitrary JavaScript.> -> <Any string that is written to an HTML‑embeddable output must be passed through an HTML entity encoder (e.g., `he.encode`) before being written to the file.>
